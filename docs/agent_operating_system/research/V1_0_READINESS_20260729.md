# v1.0 Readiness, Fourth Pass — 2026-07-29

Status: **PLANNING ONLY — EVIDENCE INDEX, NOT AUTHORIZATION**

Authority remains `02_V1_0_DEFINITION.md`; current phase state remains
`05_VERIFIED_LONG_TERM_PLAN.md` §5. This report does not sign off v1.0, supply
the Phase 7 authorization sentence as an active instruction, create an
execution token, or permit the Phase 9 call.

## Result

v1.0 is **not complete**. Phase 3–6 contract, approval, evidence, and read-only
display work remains ready. Phase 7 has design, a package specification,
canonical hash helpers, in-memory chain evidence, and rollback preview only;
the append-only writer and observed local audit file do not exist. Phase 9
remains blocked by Phase 7 completion, synchronous Owner presence, and its own
separately designed single-use gate.

NIGHT-BATCH-17 adds stronger evidence but closes neither gate:

- 90 temporal/sequence mutations fail closed across created-at order,
  predecessor hash, and monotonic IDs;
- all twelve rehearsal steps have a negative case proving immediate rejection
  and no construction of later steps;
- approval/evidence builders remain deterministic at extreme legal Unicode,
  size, nesting, and timestamp values;
- the three-source report contract is locked bidirectionally against schema and
  output drift; and
- all 50 repository fixtures have a byte-level independent SHA-256 inventory.

Package 7 also found that a board-internal hardlink to a board-external valid
fixture is accepted by the current reader on Windows. No weak or expected-fail
test was added. This is an open board-directory boundary risk: until separately
resolved, a Phase 9 preparation must not treat an untrusted or link-bearing
directory as an authoritative board. This report does not unilaterally add or
remove a frozen v1.0 inclusion item.

## Frozen inclusion status

| v1.0 inclusion | State | Current evidence | Remaining gate or risk |
|---|---|---|---|
| Blackboard contract | Ready, non-writing | Ten message schemas, validator, mutation/sequence suites, full-chain negatives, 50-fixture byte inventory | No formal board writer/runtime; hardlink boundary remains open |
| Owner approval packet | Ready, inert data | Closed packet, null token, builder, extreme/golden vectors, display and anti-dispatch tests | Approval remains decision data, never execution |
| Dry-run evidence | Ready, mock-only | Evidence schema/builder, independent hashes, tamper/extreme tests and mock-gateway rehearsal | No real side effect or permission follows |
| Local audit write | **BLOCKED** | 07 design, package draft, canonical hash chain and rollback preview | Active exact Owner sentence, writer package, observed local-file review and closeout |
| Rollback preview | Ready only as description | Pure builder, closed preview schema, cross-source and full-chain rejection tests | No Git action; v1.1 commit binding remains undecided |
| One harmless real query | **BLOCKED** | 09 runbook, 12-condition catalog, all-of truth table and negative rehearsal | Phase 7 complete, Owner present, exact N=1 action, fresh token/gate, one call and closeout |

## Shortest path to v1.0 — text timeline

```text
Owner active Phase 7 authorization sentence
  -> Phase 7 implementation package
  -> fresh-context adversarial review
  -> Owner inspects and signs off actual audit file/chain
  -> Phase 9 preparation and all preflight conditions green
  -> Owner-present N=1 harmless query
  -> Owner v1.0 evidence/sign-off decision
```

Durations below are planning estimates, not deadlines or permission.

| Step | Who | Estimated focused time | Current blocking condition / completion evidence |
|---:|---|---:|---|
| 1. Give the Phase 7 sentence in the active implementation turn | Owner | under 5 minutes | Must be verbatim: `允許寫入 data/audit_dev.jsonl（local dev append-only）`; this document's quotation is inert |
| 2. Implement the one-file Phase 7 package | Codex under the exact instruction | 2–4 hours | Only the approved writer/tests/synthetic fixtures and one local observation target; any second path, route, queue, runtime, token, or dispatch need causes HOLD |
| 3. Adversarial review and full verification | At least two fresh-context reviewers for the high-risk boundary, coordinated by Fable 5 | 1–2 hours | All 07 §10 questions, raw pytest/mypy/diff evidence, path/symlink/concurrency/tamper negatives; disagreement returns to Owner |
| 4. Inspect actual audit file and sign Phase 7 closeout | Owner | 15–30 minutes | Owner reads the real bytes/events/hash chain; test fixtures or review prose cannot substitute |
| 5. Prepare Phase 9 N=1 session | Fable 5/Codex for checks; Owner for action and gate choices | 1–2 hours plus any unresolved design review | Phase 7 complete; every preflight condition green; hardlink-board risk resolved or excluded by a reviewed trusted input path; token remains null until separately authorized contract work |
| 6. Run exactly one harmless query | Owner synchronously present; strongest available execution reviewer | 30–60 minutes | Fresh exact packet/token, one approved query, no retry, complete evidence/audit/validation, return to all-disabled state |
| 7. Decide v1.0 completion | Owner with Fable 5 evidence summary | 15–30 minutes | Both Phase 7 and Phase 9 closeouts accepted; no unresolved failure or ambiguous side effect |

The shortest path is sequential at the two Owner gates. More nightly docs/tests
can improve preparation but cannot replace steps 1, 4, 6, or 7.

## Phase 7 implementation boundary after a future authorization

The exact future whitelist and acceptance checklist remain in
`PHASE7_IMPL_PACKAGE_SPEC.md` and the third readiness pass. Quoting the
sentence here records the gate only. Until the Owner places it in the active
implementation instruction, the allowed action is **HOLD** and no writer,
formal `data/` directory, or audit file may be created.

Writer success may return evidence only. It must not enqueue, claim, dispatch,
execute, call OpenClaw/Hermes/a connector, change task state, create follow-up,
or make Phase 9 ready by itself.

## Four adversarial anti-misread statements

1. **Ready is not authorized.** A green contract, golden vector, coverage
   report, or readiness table is evidence only and cannot grant a write or
   execution permission.
2. **The quoted Phase 7 sentence is not an active Owner instruction.** It has
   effect only when Owner places it verbatim in the future implementation
   instruction for that session and exact package.
3. **Approval and audit success are not dispatch.** An approved packet, valid
   audit event, successful append, or complete chain cannot start Worker,
   OpenClaw, Phase 9, or any follow-up.
4. **A rehearsal is not a real call.** tmp-path files, mock results, golden
   hashes, a 12-step in-memory chain, and a test-memory `READY` state do not
   satisfy Phase 7 closeout or Phase 9 Owner-present execution.

## Conclusion

The system remains at the same two v1.0 gates: Phase 7 local append-only audit
writing and Phase 9 Owner-present N=1 controlled execution. The shortest path
is now explicit, but no step in that path has been pre-authorized by this
report.

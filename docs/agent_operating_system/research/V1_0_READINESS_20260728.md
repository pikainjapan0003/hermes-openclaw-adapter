# v1.0 Readiness, Third Pass — 2026-07-28

Status: **PLANNING ONLY — EVIDENCE INDEX, NOT AUTHORIZATION**

Authority remains `02_V1_0_DEFINITION.md`, with current phase state in
`05_VERIFIED_LONG_TERM_PLAN.md` §5. This report does not sign off v1.0, supply
the Phase 7 authorization sentence, create an execution token, or permit the
Phase 9 call.

## Result

v1.0 is **not complete**. Phase 3–6 contract, approval, evidence, and read-only
display work is present. Phase 7 still has design and in-memory hash/rollback
support only; its local append-only writer does not exist. Phase 9 remains
blocked behind Phase 7 completion, synchronous Owner presence, and its own
single-use execution gate.

The 2026-07-28 additions strengthen evidence without changing that result:

- 120 deterministic cross-message ID/flag/version mutations fail closed;
- the full 12-step rehearsal independently recomputes every output and
  cross-checks approval/evidence golden vectors;
- all 4,095 non-all-green states in the 12-condition preflight truth table are
  `BLOCKED`; and
- mixed board schema versions are preserved visibly per entry, but the current
  reader does not enforce board-wide version uniformity. That behavior is a
  contract risk, not Phase 7 or Phase 9 progress.

## Frozen inclusion status

| v1.0 inclusion | State | Current evidence | Remaining gate |
|---|---|---|---|
| Blackboard contract | Ready, non-writing | Ten message schemas, validator, board reader, fixture and mutation suites | No formal Blackboard writer/runtime is authorized |
| Owner approval packet | Ready, inert data | Closed packet, null token, builder, display and anti-dispatch tests | Approval remains decision data, never execution |
| Dry-run evidence | Ready, mock-only | Evidence schema/builder, independent golden hashes and tamper tests | No real side effect or permission follows |
| Local audit write | **BLOCKED** | `07_AUDIT_WRITE_DESIGN.md`, in-memory canonical hash chain and rollback preview | Exact Owner sentence, writer package, real local-file inspection and closeout |
| Rollback preview | Ready only as description | Pure builder, closed preview schema and cross-source rejection tests | No Git action; v1.1 commit binding remains undecided |
| One harmless real query | **BLOCKED** | Runbook, all-of preflight and 4,095-state negative truth table | Phase 7 complete, Owner present, fresh exact-scope token/gate, one call and closeout |

## PLANNING ONLY — Phase 7 package shape after a future authorization

The following section answers what could be dispatched **if and only if** a
future active Owner instruction contains this exact sentence:

`允許寫入 data/audit_dev.jsonl（local dev append-only）`

Quoting that sentence here records the gate; it does not satisfy it. A nightly
batch, prior approval, “continue”, or a reference to this file is insufficient.

### Exact implementation whitelist

The future package may contain only:

1. new `app/audit_writer_local.py`;
2. new writer-specific tests, expected to be
   `tests/test_audit_writer_local.py`;
3. new synthetic fixtures under a dedicated
   `fixtures/audit_writer_local/` directory, only if tests require them; and
4. the runtime observation target `data/audit_dev.jsonl`, created or appended
   only during the explicitly authorized local-dev rehearsal and inspected by
   Owner. It is not a substitute test fixture or general Blackboard path.

Any need to change an existing schema, `app/main.py`, a route, queue, worker,
approval behavior, token contract, runtime connector, remote surface, or a
second persistence path makes the package HOLD. Dependencies, deployment
files, and production/shared data are outside the whitelist.

### Required acceptance evidence

The future package is not complete unless all of the following are shown with
raw command output and file/diff evidence:

- active instruction contains the exact sentence above and work starts from
  the reviewed design commit on a non-master branch;
- writer resolves one repository-owned constant path and rejects arguments,
  environment overrides, traversal, symlinks, and alternate targets;
- import, construction, validation, chain verification, and rollback preview
  perform no write;
- every input first passes the closed `audit_event` schema and the existing
  chain is fully verified before append;
- canonical bytes obey `07_AUDIT_WRITE_DESIGN.md` §4: dict root, Unicode NFC,
  sorted keys, compact UTF-8 JSON, no floats/duplicate keys, and physical LF
  excluded from the hash;
- genesis, second entry, multi-entry, tamper, reorder, middle deletion,
  malformed line, missing final LF, duplicate IDs, and supported-platform
  concurrent append cases are tested;
- any pre-append failure leaves the file byte-for-byte unchanged, and an
  ambiguous append result never retries automatically;
- no test claims clean suffix-truncation detection without a trusted expected
  tail;
- AST/import checks prove no queue, claim, worker, dispatch, OpenClaw, Hermes,
  connector, HTTP, subprocess, route, token, or next-step permission path;
- complete pytest and mypy validation is green; `git diff --check` is empty;
- strongest-available fresh-context review checks all 15 questions in
  `07_AUDIT_WRITE_DESIGN.md` §10; and
- Owner inspects the actual `data/audit_dev.jsonl` bytes and chain before
  Phase 7 status can change to complete.

The writer's success may return evidence only. It must not enqueue, dispatch,
execute, call a model/tool, change task state, create a follow-up, or make
Phase 9 ready by itself.

## Four adversarial anti-misread statements

1. **Ready is not authorized.** A green contract, golden vector, coverage
   report, or readiness table is evidence only and cannot grant a write or
   execution permission.
2. **The quoted Phase 7 sentence is not an active Owner instruction.** It has
   effect only when Owner places it verbatim in the future implementation
   instruction for that session and exact package.
3. **Approval and audit success are not dispatch.** An approved packet, valid
   audit event, successful append, or complete chain cannot start Worker,
   OpenClaw, Phase 9, or any follow-up.
4. **A rehearsal is not a real call.** tmp-path files, mock results, golden
   hashes, a 12-step in-memory chain, and a test-memory `READY` state do not
   satisfy Phase 7 closeout or Phase 9 Owner-present execution.

## Conclusion

The system remains at the same two v1.0 gates: Phase 7 local append-only audit
writing and Phase 9 Owner-present N=1 controlled execution. This report makes
the future Phase 7 package reviewable but does not make it usable.

# v1.0 Readiness, Seventh Pass — 2026-08-04

Status: **PLANNING ONLY — EVIDENCE INDEX, NOT AUTHORIZATION**

Authority remains `02_V1_0_DEFINITION.md`; phase state remains
`05_VERIFIED_LONG_TERM_PLAN.md` §5. This report does not sign off v1.0, supply
the Phase 7 authorization sentence as an active instruction, create an
execution token, or permit the Phase 9 call.

## Current result after accepted NIGHT-BATCH-20

v1.0 is still **not complete**. Accepted master is `7cd2d98`, containing the
NIGHT-BATCH-20 closeout. NIGHT-BATCH-21 is an unmerged evidence and maintenance
branch at this report's point in time; its completed packages do not change a
phase gate.

The current verified position is:

- the default fast suite and explicit full suite remain separate and guarded;
- the schema-document renderer reports the accepted nullable and composed
  types without `unspecified` fields;
- Blackboard board-root trust follows the F7 resolved-root realpath invariant;
- the Phase 7 writer is still absent and unauthorized;
- `single_use_execution_token` remains schema-locked to null; and
- Phase 9 still requires Phase 7 closeout, separate gate/token authorization,
  and the Owner synchronously present.

NIGHT-BATCH-21 has prepared proposals for schema-version evolution and the
cross-builder relationship, but their Owner decision fields remain blank.
Package 5 and package 10 remain HOLD because their briefs contradicted the
actual repository contracts or behavior; neither HOLD was converted into a
weaker implementation.

## Frozen inclusion status

| v1.0 inclusion | State | Verifiable artifact now | Current actual blocker |
|---|---|---|---|
| Blackboard contract | Ready, non-writing | `docs/schemas/blackboard/INDEX.md`; validators; reader, mutation and boundary suites | No authorized writer/runtime |
| Owner approval packet | Ready, inert data | schema, fixtures, builder and golden vectors | Decision data never dispatches; token remains null |
| Dry-run evidence | Ready, mock-only | evidence-bundle schema, builder, mock-gateway chain and golden vectors | No real side effect follows |
| Local audit write | **BLOCKED** | `07_AUDIT_WRITE_DESIGN.md`; `research/PHASE7_IMPL_PACKAGE_SPEC.md`; negative preflight gates | The active Owner authorization sentence has not been issued |
| Rollback preview | Ready as description only | rollback-preview builder, hash-chain tests and vectors | No Git action; RB choice remains blank |
| One harmless real query | **BLOCKED** | `09_N1_PREFLIGHT_RUNBOOK.md`; preflight catalog and negative suites | Phase 7 is not closed and Owner-present execution has not been authorized |

## Shortest path, estimated time and sole gatekeeper

Times are planning estimates, not permission, promises, or acceptance SLAs.
“Sole gatekeeper” is the one role whose required action cannot be substituted
by a model, report, test, or another role.

| Step | Estimated elapsed time | Sole gatekeeper | Current actual blocker | Completion evidence |
|---:|---:|---|---|---|
| 1. Issue the active Phase 7 instruction | 2–5 minutes | **Owner** | The verbatim authorization has not been issued in an active implementation instruction | A new Owner instruction containing `允許寫入 data/audit_dev.jsonl（local dev append-only）` and its exact whitelist |
| 2. Implement the one-file writer package | 2–4 hours | **Owner-authorized Codex construction agent** | Step 1 is absent | One reviewed branch commit limited to the authorized writer, tests, fixtures and local append target |
| 3. Run adversarial Phase 7 verification | 1–2 hours | **Fable 5 as review coordinator** | No implementation exists to review | Independent review reports plus full pytest, mypy, diff, tamper, path and concurrency evidence |
| 4. Close Phase 7 | 10–30 minutes | **Owner** | Steps 2–3 are incomplete | Owner inspection of actual file bytes and verified chain, then explicit closeout |
| 5. Prepare the Phase 9 session | 30–60 minutes | **Fable 5 as preflight coordinator** | Phase 7 is not closed | Fresh catalog report with every prerequisite green and one exact harmless target proposed for Owner review |
| 6. Perform the one harmless query | 15–30 minutes | **Owner, synchronously present** | No authorized one-use gate/token exists and Owner-present session has not begun | One authorized session, one call/no retry, result and audit evidence, then return-to-disabled proof |
| 7. Decide v1.0 completion | 10–20 minutes | **Owner** | Phase 7 and Phase 9 closeouts do not exist | Explicit sign-off referencing both accepted closeouts in the §5 state record |

The construction agent in step 2 has no authority to start before step 1.
Fable 5 coordinates evidence in steps 3 and 5 but cannot replace either Owner
decision. OpenClaw may perform the single scoped call in step 6 only inside the
Owner-present session; that executor role does not own the gate.

## Four adversarial anti-misread statements

1. **Ready is not authorized.** A green contract, golden vector, coverage
   report, or readiness table is evidence only and cannot grant a write or
   execution permission.
2. **The quoted Phase 7 sentence is not an active Owner instruction.** It has
   effect only when Owner places it verbatim in the future implementation
   instruction for that session and exact package.
3. **Approval and audit success are not dispatch.** An approved packet, valid
   audit event, successful append, or complete chain cannot start Worker,
   OpenClaw, Phase 9, or any follow-up.
4. **A rehearsal is not a real call.** tmp-path files, mock results, golden
   hashes, a 12-step in-memory chain, and a test-memory `READY` state do not
   satisfy Phase 7 closeout or Phase 9 Owner-present execution.

## Conclusion

The only v1.0 blockers remain the Owner-gated Phase 7 local append-only audit
write and the Owner-present Phase 9 N=1 call. NIGHT-BATCH-21 improves evidence
and maintenance controls on an unmerged branch; it creates neither the writer
nor an execution path.
